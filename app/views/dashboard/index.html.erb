<% if logged_in? %>
  <%= render "request_cycle" %>
  <%= render "grid_request_dashboard" %>
<% else %>
  <div class="center jumbotron">
    <h1><%= t "home.title" %></h1>
    <h2><%= t "home.content" %></h2>
  </div>
<% end %>

