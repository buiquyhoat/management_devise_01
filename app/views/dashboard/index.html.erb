<% if logged_in? %>
  <div class="row">
  <%= render "chart_overview" %>
  </div>
  <div class="row">
  <%= render "top_overview" %>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1><%= t "home.title" %></h1>
    <h2><%= t "home.content" %></h2>
  </div>
<% end %>
